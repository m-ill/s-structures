# Sealed evidence uses byte hashes, not normalized text hashes.
verification/** -text
reports/representative-building-calculation-packages/**/calculation-package.json filter=lfs diff=lfs merge=lfs -text
