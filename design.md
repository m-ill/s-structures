# S-Structures UX 설계 기준

상태: UX 공통 이관 적용 / 2026-09-13. 현행 디자인 유지. 아래 기준 중 구현된 범위와 남은 확장은 [구현 기록](docs/ux/IMPLEMENTATION_20260913.md)을 따른다.

## 1. 목적과 유지할 기준선

기능 추가로 분산된 진입점·창 배치·결과 안내를 구조설계 업무 순서에 맞게 정돈한다. 기존 모델링 / 탄성해석 / 비선형해석 / 태블릿메모 탭, 왼쪽 구조 도구, 중앙 모델 캔버스, 청색 상단 바와 금색 선택 강조를 유지한다. 별도 앱 또는 새 내장 챗봇을 만들지 않는다. 사람과 외부 AI는 같은 모델·서비스·준비된 결과를 사용한다.

현행 색상은 index.html의 --dku #00467F, --dku2 #0A5CA8, --dku3 #1268B3, --gold #E8A11C, --paper #fbfcfe를 기준으로 한다. 기존 한국어 서체·경계선·둥근 모서리 계열을 이어간다. 전면 색상 변경이나 장식용 대시보드는 범위 밖이다.

최신 기능이 실제 화면에 존재하는 것과 사용자가 쉽게 발견할 수 있는 것은 구분한다. 감사 근거는 [UX 점검 보고서](docs/ux/UX_AUDIT_20260913.md)에 있다.

## 2. 정보 배치

- 상단: 프로젝트/현재 작업 탭/공통 실행취소·뷰. 저작권은 정보 창 또는 공간이 허용되는 하단으로 이동한다. 제목을 세로 한 글자씩 감싸지 않는다.
- 리본: 현재 업무에서 자주 쓰는 작업. 상시 펼친 버튼을 계속 추가하지 말고 기능 그룹과 더보기를 사용한다.
- 왼쪽: 모델링 도구와 선택 부재의 재료·단면 접근. 재료와 단면의 만들기/편집/선택 적용을 대칭으로 제공한다.
- 오른쪽 작업 패널: 선택 대상의 속성·검토·보완 맥락. 모델을 보면서 반복하는 작업에 사용한다.
- 넓은 작업 창: 많은 입력, 결과 비교, 계산서 미리보기. 동일한 창 프레임에 내부 탐색을 둔다.
- 메뉴: 프로젝트 열기/저장/복원, 가져오기/내보내기, 작업공간, 도움말. 삭제 계열은 저장 계열과 시각적으로 분리한다.
- 상태: 현재 모델/선택 해석/검토 현재성/진행 상태를 짧게 표시한다. WebMCP 등록 수·해시·Worker 진단·개발 단계명은 ‘연결 및 진단’ 상세로 보낸다. 미지원·미검토·오래된 결과 같은 의사결정 정보는 숨기지 않는다.

## 3. 명령과 진입점

아래는 새 수치 기능 개발이 아니라 기존 서비스의 UI 접근성 개선이다.

| 업무 | 주 진입점 제안 | 내부 화면 | 현행 서비스 유지 |
|---|---|---|---|
| 재료·단면 | 모델링 > 재료·단면 | 등록/편집/선택 적용 | practical 입력 계약·라이브러리 |
| 배근·접합·기초·지반 | 탄성해석 > 설계 입력 | 종류별 탭, 선택 대상 반영 | design input preview/apply |
| 1차·P-Delta·동적 해석 | 탄성해석 > 해석 | 케이스/조합/실행 | 기존 elastic workflow |
| RC 강성·장기변형 | 탄성해석 > 사용성 검토 | 강성/부착 후 변형/결과 | rcServiceWorkflow |
| 부재·접합·기초 검토 | 탄성해석 > 상세 검토 | 대상·실패 사유·근거 | practical evaluation |
| 자동 보완 | 상세 검토 > 보완안 | 후보/비교/미리보기/적용/재검토 | 기존 candidate workflow |
| 이음 전달 | 사용성·상세 검토 > 이음 | 해석/결과 연결 | splice workflow |
| 계산서·도면·수량 | 탄성해석 > 보고서 | 출력 종류별 선택 | 준비 결과·artifact export |
| 작업 저장·복원 | 메뉴 > 프로젝트 | 파일/브라우저 저장 구분 | native persistence/checkpoint |
| AI 상태·KDS 지원 | 메뉴 > 연결 및 진단 / 검토 근거 | 현재 지원/출처/제약 | WebMCP/capability 조회 |

주요 기능은 관련 탭에서 최대 두 번의 명시적 선택으로 도달하게 한다. 버튼은 목적별로 한 대표 진입점을 가지되, 선택 부재의 문맥 바로가기는 허용한다. 실행되지 않는 기능을 장식 버튼으로 추가하지 않는다. 비활성 버튼에는 다음에 필요한 입력과 해당 입력 화면 링크를 제공한다.

공통 명령 메타데이터 제안: id, label, group, panelId, sectionId, availability/reason, requiredSelection. 메뉴/리본/문맥 바로가기는 같은 명령을 호출한다. UI에서 해석식을 재구현하지 않으며 WebMCP 도구 ID·입력 스키마는 문구 정리 때문에 변경하지 않는다.

## 4. 창과 팝업 규약

| 유형 | 기본 배치 | 기억할 상태 | 종료 방식 |
|---|---|---|---|
| 메뉴/더보기 | 누른 버튼의 아래, 화면 경계 안으로 보정 | 위치 저장 안 함 | 바깥 클릭/Esc |
| 속성/검토 보조 패널 | 작업영역 오른쪽, 동일 panelId로 복원 | 사용자가 이동·크기 변경한 위치 | 고정 헤더 닫기 |
| 입력/비교 작업 창 | 공통 넓은 창 프레임, 현재 작업영역 기준 중앙 | 종류별 위치·크기, 입력 초안 별도 | 고정 헤더/하단 닫기 |
| 확인 대화상자 | 활성 부모 창 기준 중앙 | 위치 저장 안 함 | 확인/취소/Esc |

기존 floatingPanel/workspace controller를 재사용하되, 모달 포커스 관리까지 있다고 가정하지 않는다. 새로운 공용 창 어댑터에서 모달/비모달을 명확히 구분한다.

- 저장 위치는 panelId·레이아웃 버전으로 식별하고 창 열기·resize 때 작업영역 안으로 clamp한다. 화면 크기가 바뀌어도 제목/닫기/주 작업 버튼이 경계 밖으로 나가지 않는다.
- 사용자가 옮긴 위치를 매번 강제 중앙 정렬하지 않는다. 메뉴에는 ‘창 배치 초기화’를 둔다. 기존 저장값은 검증·마이그레이션하고 깨진 값만 기본 위치로 복구한다.
- 헤더와 주 작업 footer는 고정하고 본문만 스크롤한다. 긴 검토 창의 끝까지 내려가야 닫을 수 있는 구조를 없앤다.
- 중앙 창·보조 패널·메뉴·확인창의 z-index는 공통 계층으로 관리한다. 1200/19000/19010 같은 개별 상수를 추가하지 않는다.
- 실제 모달만 aria-modal와 포커스 순환/배경 비활성화를 적용한다. 닫으면 진입 버튼에 포커스를 돌린다. 비모달 패널은 캔버스 조작을 허용한다.
- 작업 중 닫기와 취소를 구분한다. UI를 닫았다고 Worker가 취소되었다고 표시하지 않는다. 늦은 응답이 재개한 화면을 덮어쓰지 않도록 기존 수명주기 가드를 유지한다.

## 5. 폼·검토·출력

폼은 ‘대상 선택 → 필수 입력 → 선택 입력 → 근거/고급’ 순서로 구성한다. 재료는 기본 물성/강도/시간 의존 입력을 구분하고, 장기변형은 재하→부착→최종 시점을 같은 행 또는 순서로 배치한다. 필수값·미지정·0의 의미를 보존한다.

부착 후 변형 검토는 부재와 조합을 유효 목록에서 선택하고 선택 결과의 재령을 표시한다. 원본 상태를 임의 변경하거나 허용값을 추정하지 않는다. 내부 m 계약은 유지하고 화면에서 mm를 제공할 경우 명시적 변환 어댑터와 단위 시험을 둔다. 재료 입력으로 가는 바로가기, 부착/최종 실행 순서, 결과 없음 사유, 계산서 복사/저장 접근을 제공한다.

RC·접합·기초 보완 조건의 일반 작업은 표/선택 입력으로 제공하고 원시 JSON은 고급 편집으로 접는다. 고급 입력을 제거하지 말고 필드 입력과 왕복 가능한 공통 계약을 사용한다. 후보 탐색 실패를 숨기지 않는다.

검토 화면 내부 탐색: 개요 / 부재 / 접합·기초 / 사용성·장기변형 / 보완안 / 계산서·도면. 한 화면에 모든 입력과 실행 버튼을 이어 붙이지 않는다. 현재 선택·필터·결과 출처는 탭을 바꾸어도 유지한다. NG와 미검토를 분리하고 각 행에서 해당 입력을 열도록 한다.

일반 출력은 수치표·단위·판정·지배 조건·KDS 근거를 먼저 보여준다. JSON·해시·엔진 버전은 근거 상세에 둔다. ‘전체 OK’는 대상과 검토 범위를 함께 표시하며 간이 캔버스 결과와 저장 해석 결과를 같은 상태처럼 표현하지 않는다. 수치 OK를 KDS 전체 승인으로 승격하지 않는다.

보고서 선택에서는 화면 PDF / 검토 계산서 / 도면·수량 / 전권 묶음의 차이를 설명한다. 준비된 동일 결과를 렌더링하며 UI용 파생 계산을 새로 만들지 않는다.

## 6. 문구와 스타일

| 현재 예 | 표시 제안 | 비고 |
|---|---|---|
| Phase 13 실무 워크벤치 열기 | 실무 워크벤치 열기 | 개발 단계는 제품 정보로 |
| ratio | 검정비 표시 | 실제 동작과 범위 대조 후 확정 |
| Scale / auto | 변형 배율 / 자동 | 선택값 계약은 유지 |
| spring / settle / temp | 탄성지지 / 강제변위 / 온도하중 | 실제 기능 의미에 맞춰 확정 |
| Calculation Package | 계산 패키지 | 출력 종류 명시 |
| Reset workspace | 창 배치 초기화 | 저장 데이터 삭제와 구분 |
| JSON 가져오기 (v1 2D 호환) | 프로젝트 열기 | 지원 형식은 파일 안내로 |
| WebMCP unsupported · 도구 0개… | AI 연결 미지원 | 전체 진단은 상세로 |

M/Q/N/T 약어는 전문가 단축 표기를 유지할 수 있으나 툴팁과 접근성 이름에 의미를 제공한다. 버튼 높이는 기본 32~36px, 터치 모드 44px 이상을 구현 목표로 한다. 패딩·테두리·폰트·focus 스타일을 공통화하여 브라우저 기본 회색 버튼이 리본에 섞이지 않도록 한다.

## 7. 구현 마일스톤과 완료 조건

| 단계 | 구현 범위 | 최소 확인 / 완료 조건 |
|---|---|---|
| UX-M0 기준선 | 현황·캡처·design.md·기능 진입점 대조 | 이번 문서 준비 완료. 전 화면 완주 판정 아님 |
| UX-M1 창 배치 | 공용 창 프레임, 입력/검토/단면/기존 패널 연계, 메뉴 앵커, 위치 복원 | 열기/닫기/다시열기/resize/배치 초기화, 닫기 버튼 가시성; 좌표 clamp 단위 시험 |
| UX-M2 리본·메뉴 | 명령 목록, 문구·상태 분리, 더보기, 표준 버튼 | 기존 action 도달성 유지, 1440/1024 화면 줄바꿈·잘림 캡처, 중복/비활성 사유 확인 |
| UX-M3 입력·바로가기 | 재료·단면 대칭, 배근/접합/기초/장기변형 진입, 폼 그룹 | 선택 부재·조합 전달, 필수값/단위·0 보존, 기존 preview/apply 계약 시험 |
| UX-M4 검토·보완·출력 | 내부 탭·표·필터·사유→입력, 보고서 선택, 고급 JSON 접기 | 동일 준비 결과/출처 유지, 저장 결과 재열기, export·취소·늦은 응답 최소 시험 |
| UX-M5 통합 마감 | 키보드/태블릿·레이아웃 복원, WebMCP 공통 결과, 안내서·전후 캡처 | 대표 합성 모델 1건만 후속 종합검증과 조율; 수치 전체 회귀/실제 건물/배포는 별도 |

단계마다 변경 전/후 동일 viewport 캡처, 실제 수정 파일·선택 테스트·미충족 항목을 기록한다. 함수 이름과 테스트 범위는 구현 시 현재 코드로 확정한다. M1~M5의 공통 이관과 선택 검증을 적용했다. 전용 조건 편집기·원본 재령 자동 채움·전면 결과 표 재작성은 범위 밖으로 남긴다. 구현 기록의 실제 파일·검증 범위를 완료 근거로 사용한다.

## 8. 구현 시 파일 경계

indexNativeRibbon / indexElasticAnalysisRibbon: 명령 그룹과 리본.
indexDesignInput / indexDesignReview / indexPracticalDesign: 입력·검토 창과 내부 탐색.
rcServiceControls / rcAttachmentReviewControls / splice*Controls / drawingArtifactControls: 업무별 하위 뷰.
floatingPanel / phase7WorkspaceController / workspaceState: 위치 복원과 작업영역.
webmcp/register: 짧은 연결 상태와 상세 진단.

신규 공용 창/명령/폼 스타일 모듈을 먼저 도입하고 기존 화면을 순차 이관한다. index.html에 화면별 위치 CSS를 계속 누적하지 않는다. 구형 창과 신형 창이 한 기능을 이중 실행하지 않도록 owner를 하나로 유지한다.
