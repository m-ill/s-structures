import {evaluateColumnTransfer} from '../src/design/foundation/columnTransfer.js';
import {resolveFootingLoadLedger} from '../src/design/foundation/footingLoadLedger.js';
import assert from 'node:assert/strict';
import {stagePracticalDesignInput} from '../src/modeling/practicalDesignInputs.js';
import {validateModel} from '../src/core/validation.js';
import {designContext} from './fixtures/p24/context.js';
import {buildDetailDrawings} from '../src/report/phase24/detailDrawings.js';
import {validateStoredDesignDetails} from '../src/modeling/designDetailValidation.js';
const ctx=designContext(),m=ctx.model;
try{
 m.nodes=[{id:'A',x:0,y:0,z:0,support:'fixed'},{id:'B',x:0,y:0,z:3}];m.members=[{id:'AB',type:'frame',n1:'A',n2:'B',matId:'concrete',secId:'rc3060'}];m.loadCases=[{id:'D',type:'dead',name:'D'}];m.loads=[{id:'F',type:'nodal',node:'B',P:10,dir:'-z',case:'D'}];m.loads.push({id:'H',type:'nodal',node:'B',P:2,dir:'+x',case:'D'});m.loadCombinations=[{id:'U',type:'strength',name:'U',factors:{D:1.4}}];m.analysisCases=[{id:'E',name:'E',kind:'static',status:'not-run',settings:{comboId:'U',pDeltaMethod:'off'}}];
 const ground={type:'ground-record',id:'G',name:'synthetic',version:1,sourceNote:'test',sourceReference:'fixture',basisStatus:'specified',allowableBearing:150,bearingBasis:'gross'};
 const columnBars={type:'reinforcement-record',id:'R',name:'continuous column bars',version:1,sourceNote:'synthetic',memberId:'AB',start:0,end:1,cover:.04,barMaterialId:'steel@1',reinforcementForm:'single-deformed',barCoating:'uncoated',lapRequired:false,bars:[-1,1].flatMap(y=>[-1,1].map(z=>({y:y*.2,z:z*.08,diameter:20})))};
 const footing={columnOffsetX:.2,columnOffsetY:-.1,reactionMomentReference:'column-center',columnTransferType:'cast-in-place-continuous-straight-bars',columnMemberId:'AB',columnEmbedmentLength:.3,columnDevelopmentAbove:.3,barShape:'straight',shrinkageRestraint:'ordinary-not-severely-restrained',type:'foundation-record',id:'F',name:'synthetic',version:1,sourceNote:'test',nodeId:'A',foundationType:'isolated',B:2,L:2,thickness:.5,cover:.05,materialId:'concrete@1',groundId:'G@1',barMaterialId:'steel@1',bottomDiameterB:16,bottomDiameterL:16,bottomSpacingB:150,bottomSpacingL:150,topDiameterB:16,topDiameterL:16,topSpacingB:150,topSpacingL:150,footingWeightCaseId:'D',reactionBasis:'superstructure-only',columnWidth:.6,columnDepth:.3,flexureStandard:'KDS-142020-2022',concreteWeight:'normal',barCoating:'uncoated',punchingStandard:'KDS-142022-2022',punchingMomentMethod:'conservative-perimeter-shear',punchingPerimeterScope:'interior-solid-no-openings'};
 assert.ok((await ctx.call('get_design_input_schema',{type:'foundation-record'})).schema.properties.topDiameterB);
 const staging=structuredClone(m);for(const c of [ground,columnBars,footing])stagePracticalDesignInput(staging,c,[]);assert.equal(validateModel(staging).ok,true,JSON.stringify(validateModel(staging)));
 const preview=await ctx.call('preview_design_changes',{inputHash:ctx.bridge.getWorkflowInputIdentity().inputHash,requestId:'footing-preview',commands:[ground,columnBars,footing]});
 assert.equal((await ctx.call('apply_design_changes',{handle:preview.handle,requestId:'footing-apply'})).ok,true);
 const stored=(await ctx.call('get_design_records',{channel:'foundations',id:'F'})).rows[0];assert.equal(stored.reinforcement.topB.diameter,.016);assert.deepEqual(validateStoredDesignDetails(m),[]);
 const run=await ctx.bridge.runElasticWorkflow({plan:ctx.bridge.planElasticWorkflow({caseIds:['E']}),requestId:'footing-source'});assert.equal(run.ok,true,JSON.stringify({run,error:ctx.bridge.getAnalysisLatestAttempt('E')?.error}));
 const result=await ctx.call('evaluate_practical_design',{inputHash:ctx.bridge.getWorkflowInputIdentity().inputHash,sources:[{analysisRunId:run.steps[0].analysisRunId,comboId:'U'}]});
 const missing=ctx.bridge.getPracticalDesignSnapshot(result.evaluationId).checks.find(r=>r.checkId==='foundation-flexure');assert.equal(missing.reason,'FOOTING_REACTION_HEIGHT_REFERENCE_REQUIRED');assert.equal(ctx.bridge.getPracticalDesignSnapshot(result.evaluationId).checks.find(r=>r.checkId==='foundation-column-transfer').reason,missing.reason);
 const schema=(await ctx.call('get_design_input_schema',{type:'foundation-record'})).schema;assert.ok(schema.properties.reactionVerticalReference);assert.ok(schema.properties.reactionHeightAboveBase);
 const heightPreview=await ctx.call('preview_design_changes',{inputHash:ctx.bridge.getWorkflowInputIdentity().inputHash,requestId:'height-preview',commands:[{...footing,version:2,reactionVerticalReference:'footing-top'}]});
 assert.equal((await ctx.call('apply_design_changes',{handle:heightPreview.handle,requestId:'height-apply'})).ok,true);
 const run2=await ctx.bridge.runElasticWorkflow({plan:ctx.bridge.planElasticWorkflow({caseIds:['E']}),requestId:'height-run'});
 const evaluation=await ctx.call('evaluate_practical_design',{inputHash:ctx.bridge.getWorkflowInputIdentity().inputHash,sources:[{analysisRunId:run2.steps[0].analysisRunId,comboId:'U'}]});
 const snapshot=ctx.bridge.getPracticalDesignSnapshot(evaluation.evaluationId),check=snapshot.checks.find(r=>r.checkId==='foundation-flexure'),ledger=check.loadLedger,reaction=snapshot.sets[0].set.reactions.A;
 assert.equal(ledger.reactionHeightAboveBase,.5);assert.equal(ledger.reactionVerticalReference,'footing-top');assert.ok(Math.abs(reaction.rx)>0);
 assert.ok(Math.abs(ledger.totalMy-(reaction.rmy-ledger.columnN*.2+.5*reaction.rx))<1e-9);assert.ok(Math.abs(ledger.columnMy-reaction.rmy)<1e-9);
 let text='',offset=0;do{const row=await ctx.call('get_practical_design_check',{evaluationId:evaluation.evaluationId,checkId:check.id,offset,limit:4096});text+=row.chunk;offset=row.nextOffset;}while(offset!==null);assert.deepEqual(JSON.parse(text).loadLedger,ledger);
 const plan=await ctx.call('plan_design_candidates',{evaluationId:evaluation.evaluationId,foundationId:'F',detailCandidates:[{thickness:.6}],maxCandidates:1,maxMillis:10000});
 const start=await ctx.call('start_design_candidates',{planId:plan.planId,requestId:'height-candidate'});let job;
 for(let i=0;i<500;i++){job=await ctx.call('get_design_candidates',{jobId:start.jobId});if(job.status!=='running')break;await new Promise(r=>setTimeout(r,10));}assert.ok(job.best,JSON.stringify(job));
 const receipt=await ctx.call('apply_design_candidate_and_review',{jobId:job.jobId,candidateId:job.best.candidateId,requestId:'height-candidate-apply'});assert.equal(receipt.followUp.status,'completed');
 const final=ctx.bridge.getPracticalDesignSnapshot(receipt.followUp.evaluationId),after=final.checks.find(r=>r.checkId==='foundation-flexure').loadLedger;
 assert.equal(after.reactionHeightAboveBase,.6);assert.ok(Math.abs(after.columnMy-ledger.columnMy)<1e-9);assert.ok(Math.abs(after.totalMy-ledger.totalMy-.1*reaction.rx)<1e-9);
 const latest=(await ctx.call('get_design_records',{channel:'foundations',id:'F'})).rows.find(r=>r.version===3);assert.equal(latest.reactionVerticalReference,'footing-top');assert.equal(latest.thickness,.6);
 assert.equal(final.checks.find(r=>r.checkId==='foundation-column-transfer').reason,'COLUMN_INTERFACE_PREPARATION_REQUIRED');
 console.log('PASS actual WebMCP missing height, typed top reference, stored ledger query and candidate thickness apply/reanalysis/review');
}finally{await ctx.dispose();}
