import {buildDetailDrawings} from '../src/report/phase24/detailDrawings.js';
import assert from 'node:assert/strict';
import {designContext} from './fixtures/p24/context.js';
const ctx=designContext(),m=ctx.model;
try{
 m.nodes=[{id:'A',x:0,y:0,z:0,support:'fixed'},{id:'B',x:0,y:0,z:3}];m.members=[{id:'AB',type:'frame',n1:'A',n2:'B',matId:'concrete',secId:'C@1'}];
 m.loadCases=[{id:'D',type:'dead',name:'D'}];m.loads=[{id:'F',type:'nodal',node:'B',dir:'-z',P:10,case:'D'}];m.loadCombinations=[{id:'U',type:'strength',name:'U',factors:{D:1.4}}];m.analysisCases=[{id:'E',name:'E',kind:'static',status:'not-run',settings:{comboId:'U',pDeltaMethod:'off'}}];
 const c=.13+.01/Math.sqrt(2),bars=[[-c,-c],[-.14,0],[-c,c],[0,.14],[c,c],[.14,0],[c,-c],[0,-.14]].map(([y,z])=>({y,z,diameter:20}));
 const detail={type:'reinforcement-record',id:'R',name:'synthetic column',version:1,sourceNote:'synthetic',memberId:'AB',start:0,end:1,cover:.04,barMaterialId:'steel@1',bars,stirrupDiameter:10,stirrupSpacing:200,stirrupLegs:2,memberRole:'compression-member',reinforcementForm:'single-deformed',stirrupForm:'closed-rectangular-two-leg',confinementStandard:'KDS-142050-2022',confinementSystem:'ordinary-tied-column',tieClosure:'standard-135',tieClosureCorner:'+y+z',tieClosureSeparation:0,tieHookTail:.06,tieBendInsideRadius:.02,tieFirstStart:.1,tieFirstEnd:.1,fabricationShape:'straight',endSetbackStart:.04,endSetbackEnd:.04,topAnchorBolts:false,crossTieHookSides:['left','right'],crossTiePlaneOffsets:['0.015','0.015'],crossTieBarPairs:['2:6','4:8']};
 const preview=await ctx.call('preview_design_changes',{inputHash:ctx.bridge.getWorkflowInputIdentity().inputHash,requestId:'cross-preview',commands:[{type:'section-record',id:'C',name:'400 square',version:1,shape:'RECT',dimensionUnit:'mm',B:400,H:400,sourceNote:'synthetic'},detail]});
 assert.equal((await ctx.call('apply_design_changes',{handle:preview.handle,requestId:'cross-apply'})).ok,true);
 assert.deepEqual((await ctx.call('get_design_records',{channel:'reinforcement',id:'R'})).rows[0].crossTieBarPairs,['2:6','4:8']);
 const run=await ctx.bridge.runElasticWorkflow({plan:ctx.bridge.planElasticWorkflow({caseIds:['E']}),requestId:'cross-run'});assert.equal(run.ok,true);
 const evaluation=await ctx.call('evaluate_practical_design',{inputHash:ctx.bridge.getWorkflowInputIdentity().inputHash,sources:[{analysisRunId:run.steps[0].analysisRunId,comboId:'U'}]});
 const check=ctx.bridge.getPracticalDesignSnapshot(evaluation.evaluationId).checks.find(c=>c.checkId==='rc-confinement');assert.equal(check.status,'NG',JSON.stringify(check));assert.equal(check.supports.supportedBarIndices.length,8);assert.equal(check.supports.fabricationApproved,false);assert.ok(check.codeReferences.some(r=>r.clause.includes('4.4.2(3)')));
 const snapshot=ctx.bridge.getPracticalDesignSnapshot(evaluation.evaluationId);
 const geometry=snapshot.preparedDetails.reinforcement['R@1'].crossTies;
 assert.deepEqual(check.geometry,geometry);assert.equal(geometry.pieces.length,2);assert.equal(geometry.status,'NG');assert.ok(geometry.assembly.checks.some(c=>c.kind==='cross-tie-pair'&&c.status==='NG')); // Same-plane cross ties collide.
 const drawings=buildDetailDrawings(snapshot,{maxPages:600,pageLimit:60}),qs=drawings.quantities.filter(q=>q.kind==='cross-tie');
 assert.ok(drawings.retainedPageCount<=60);assert.ok(drawings.totalPages>=drawings.retainedPageCount);
 assert.equal(qs.length,2);assert.equal(qs[0].planeOffset,.015);assert.ok(qs[0].cutLength>0);assert.ok(qs[0].count>0);assert.equal(qs[0].fabricationApproved,false);
 assert.ok(drawings.pages[0].commands.some(c=>c.text?.includes('크로스타이 형상 NG')));
 assert.deepEqual(drawings.quantities.find(q=>q.kind==='stirrup').perimeterGeometry,snapshot.preparedDetails.reinforcement['R@1'].outerHoop);
 assert.equal(snapshot.preparedDetails.reinforcement['R@1'].outerHoop.status,'OK');
 const record=(await ctx.call('get_design_records',{channel:'reinforcement',id:'R'})).rows[0];
 assert.equal(record.tieClosureCorner,'+y+z');assert.equal(record.tieClosureSeparation,0);
 const closure=snapshot.preparedDetails.reinforcement['R@1'].outerHoop.closureGeometry;
 assert.deepEqual(closure.longitudinalAssembly,snapshot.preparedDetails.reinforcement['R@1'].outerHoop.actualPathAssembly);
 assert.ok(closure.longitudinalAssembly.scope.includes('actual spatial hoop'));assert.ok(closure.longitudinalAssembly.segmentPairs>0);
 assert.equal(closure.selfAssembly.sameStation.status,'NG');assert.equal(closure.assemblyStatus,'NG');
 assert.ok(closure.crossTieAssembly.segmentPairs>0);assert.ok(closure.crossTieAssembly.checks.length===2);
 assert.equal(check.checks.filter(c=>c.kind==='spatial-tie-corner-support').length,4);
 assert.equal(check.supportOrdering.metric,'longitudinal-center-polygon');assert.deepEqual(check.supportOrdering.orderedBarIndices,[5,4,3,2,1,8,7,6]);assert.equal(closure.perimeterOrderStatus,'OK');
 assert.equal(closure.perimeterMembership.status,'OK');assert.equal(closure.perimeterMembership.contactedBarIndices.length,8);assert.equal(closure.faceContactCoverage.checks.length,4);
 assert.equal(closure.supportCoverage.status,'OK');assert.equal(new Set(closure.supportCoverage.supportedBarIndices).size,4);assert.equal(closure.supportCoverage.fabricationApproved,false);
 assert.equal(closure.contactCoverage.status,'OK');assert.equal(closure.contactCoverage.commonBarIndices.length,1);assert.equal(closure.contactCoverage.fabricationApproved,false);
 assert.equal(closure.hookPair.status,'NG');assert.equal(closure.path.status,'OK');
 assert.equal(check.incomplete,true);assert.equal(check.methodReviewRequired,true);
 assert.ok(check.incompleteReasons.includes('HOOP_CLOSURE_HOOK_COLLISION'));
 const q=drawings.quantities.find(q=>q.kind==='stirrup');
 assert.deepEqual(q.closureGeometry,closure);assert.equal(q.geometricCutLength,closure.path.centerlineLength);assert.equal(q.cutLength,null);
 assert.ok(drawings.pages[0].commands.some(c=>c.text?.includes('폐합 후크 형상 NG')));
 const plan=await ctx.call('plan_design_candidates',{evaluationId:evaluation.evaluationId,memberId:'AB',regionConstraints:[{detailId:'R',tieClosureCorners:['-y-z'],tieClosureSeparations:[.03,0]}],maxCandidates:2,maxMillis:10000});
 const start=await ctx.call('start_design_candidates',{planId:plan.planId,requestId:'closure-repair'});let job;
 for(let i=0;i<500;i++){job=await ctx.call('get_design_candidates',{jobId:start.jobId});if(job.status!=='running')break;await new Promise(r=>setTimeout(r,10));}
 assert.ok(job.best,JSON.stringify(job));assert.equal(job.candidates.length,2);
 assert.equal(job.best.candidateId,job.candidates[1].candidateId);assert.ok(job.candidates[0].summary.closureDiagnosticCounts.NG>job.candidates[1].summary.closureDiagnosticCounts.NG);
 // Explicitly exercise the first proposal; it is not promoted as the best or a complete design.
 const receipt=await ctx.call('apply_design_candidate_and_review',{jobId:start.jobId,candidateId:job.candidates[0].candidateId,requestId:'closure-repair-apply'});
 assert.equal(receipt.followUp.status,'completed',JSON.stringify(receipt));
 const after=ctx.bridge.getPracticalDesignSnapshot(receipt.followUp.evaluationId),next=after.preparedDetails.reinforcement['R@2'].outerHoop.closureGeometry;
 assert.equal(next.corner,'-y-z');assert.equal(next.separation,.03);assert.equal(next.hookPair.status,'OK');
 assert.ok(after.checks.find(c=>c.checkId==='rc-confinement').incomplete);assert.equal(next.fabricationApproved,false);
 assert.equal((await ctx.call('get_design_records',{channel:'reinforcement',id:'R'})).rows.find(x=>x.version===2).tieClosureSeparation,.03);
 const fitPlan=await ctx.call('plan_design_candidates',{evaluationId:receipt.followUp.evaluationId,memberId:'AB',regionConstraints:[{detailId:'R',closureBarFits:['contact'],crossTieCageFits:['separate']}],maxCandidates:1,maxMillis:10000});
 const fitStart=await ctx.call('start_design_candidates',{planId:fitPlan.planId,requestId:'spatial-fit'});let fitJob;
 for(let i=0;i<500;i++){fitJob=await ctx.call('get_design_candidates',{jobId:fitStart.jobId});if(fitJob.status!=='running')break;await new Promise(r=>setTimeout(r,10));}
 assert.ok(fitJob.best,JSON.stringify(fitJob));
 const fitted=await ctx.call('apply_design_candidate_and_review',{jobId:fitStart.jobId,candidateId:fitJob.best.candidateId,requestId:'spatial-fit-apply'});
 assert.equal(fitted.followUp.status,'completed',JSON.stringify(fitted));
 const fittedSnapshot=ctx.bridge.getPracticalDesignSnapshot(fitted.followUp.evaluationId),fittedClosure=fittedSnapshot.preparedDetails.reinforcement['R@3'].outerHoop.closureGeometry;
 assert.equal(fittedClosure.contactCoverage.status,'OK');assert.equal(fittedClosure.supportCoverage.status,'OK');
 assert.equal(fittedClosure.crossTieAssembly.status,'OK');
 assert.ok(fittedSnapshot.preparedDetails.reinforcement['R@3'].crossTies.assembly.checks.filter(c=>c.kind==='cross-tie-pair').every(c=>c.status==='OK'));
 assert.equal(fittedClosure.perimeterOrderStatus,'OK');assert.equal(fittedClosure.fabricationApproved,false);
 assert.ok(fittedSnapshot.checks.find(c=>c.checkId==='rc-confinement').incomplete);
 assert.ok(fittedSnapshot.checks.find(c=>c.checkId==='rc-confinement').codeReferences.length>0);
 const fittedRecord=(await ctx.call('get_design_records',{channel:'reinforcement',id:'R'})).rows.find(x=>x.version===3);
 assert.deepEqual(fittedRecord.bars.map(b=>b.diameter),bars.map(b=>b.diameter/1000));
 assert.notDeepEqual(fittedRecord.bars.map(b=>[b.y,b.z]),bars.map(b=>[b.y,b.z]));
 assert.equal(fittedClosure.diagnosticCounts.NG,0);assert.equal(fittedClosure.diagnosticCounts.NOT_CHECKED,0);
 assert.equal(fittedClosure.longitudinalAssembly.status,'OK');assert.equal(fittedClosure.selfAssembly.status,'OK');
 assert.ok(fittedSnapshot.preparedDetails.reinforcement['R@3'].crossTies.assembly.checks.every(c=>c.status==='OK'));
 const transverse=fittedSnapshot.preparedDetails.reinforcement['R@3'].transverseQuantity;
 assert.equal(transverse.status,'OK');assert.equal(fitJob.best.objective.quantityComplete,true);assert.equal(fitJob.best.objective.longitudinalBasis,'prepared-end-and-splice-pieces');
 assert.ok(Math.abs(fitJob.best.objective.value-fittedSnapshot.preparedDetails.reinforcement['R@3'].longitudinalQuantity.volume-transverse.volume)<1e-12);assert.equal(fitJob.best.objective.secondaryUnit,'m3');
 assert.ok(Math.abs(fitJob.best.objective.secondary-transverse.volume)<1e-12);
 const fittedDrawings=buildDetailDrawings(fittedSnapshot,{maxPages:600,pageLimit:60}),transverseRows=fittedDrawings.quantities.filter(q=>['stirrup','cross-tie'].includes(q.kind));
 assert.ok(Math.abs(transverseRows.reduce((sum,q)=>sum+q.geometricQuantity.volume,0)-transverse.volume)<1e-12);
 assert.equal(transverseRows.find(q=>q.kind==='stirrup').cutLength,null);
 assert.ok(fittedDrawings.pages[0].commands.some(c=>c.text?.includes('횡철근 기하 총길이')));
 const repeatPlan=await ctx.call('plan_design_candidates',{evaluationId:fitted.followUp.evaluationId,memberId:'AB',regionConstraints:[{detailId:'R',closureBarFits:['contact'],crossTieCageFits:['separate'],tieClosureSeparations:[.04]}],maxCandidates:1,maxMillis:10000});
 const repeatStart=await ctx.call('start_design_candidates',{planId:repeatPlan.planId,requestId:'cage-refit'});let repeatJob;
 for(let i=0;i<500;i++){repeatJob=await ctx.call('get_design_candidates',{jobId:repeatStart.jobId});if(repeatJob.status!=='running')break;await new Promise(r=>setTimeout(r,10));}
 assert.ok(repeatJob.best,JSON.stringify(repeatJob));
 const repeated=await ctx.call('apply_design_candidate_and_review',{jobId:repeatStart.jobId,candidateId:repeatJob.best.candidateId,requestId:'cage-refit-apply'});
 assert.equal(repeated.followUp.status,'completed',JSON.stringify(repeated));
 const repeatSnapshot=ctx.bridge.getPracticalDesignSnapshot(repeated.followUp.evaluationId),repeatClosure=repeatSnapshot.preparedDetails.reinforcement['R@4'].outerHoop.closureGeometry;
 assert.equal(repeatClosure.separation,.04);assert.equal(repeatClosure.supportCoverage.status,'OK');assert.equal(repeatClosure.perimeterOrderStatus,'OK');
 assert.equal(repeatClosure.crossTieAssembly.status,'OK');assert.equal(repeatClosure.fabricationApproved,false);
 console.log('PASS combined cage repair: spatial closure diagnostics NG=0, NOT_CHECKED=0; full KDS/detailing review remains explicit');
 console.log('PASS WebMCP typed closure input, continuous path, collision and incomplete gate, shared drawing length');
}finally{await ctx.dispose();}
