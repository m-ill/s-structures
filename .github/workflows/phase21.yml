name: Phase21 consistency validation
on:
  workflow_call:
  workflow_dispatch:
  push:
    branches: ['work/phase21-*']
permissions:
  contents: read
jobs:
  phase21:
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/setup-node@v4
        with:
          node-version: '24.16.0'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: node tools/run-p19-validation.mjs phase21-validation --manifest=verification/specs/phase21/tests.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: phase21-${{ matrix.os }}
          path: |
            phase21-validation/*.json
            phase21-validation/*.log
          retention-days: 30
