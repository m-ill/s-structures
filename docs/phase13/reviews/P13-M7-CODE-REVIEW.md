# P13-M7 코드 검토 기록

판정: implementation-complete / qualification-pending-approved-fixture. 제한 MGT subset은 byte/line/column/cell budget과 source hash·encoding·unit·parser provenance를 보존한다. 미지원 record와 mapping blocker를 모두 노출하고 현재 모델을 바꾸지 않는 candidate/draft revision만 만든다. exporter round-trip parity, immutable 검토 package와 canonical revision diff를 실제 UI에 통합했으며 외부 application·macro·formula·path·subprocess 실행은 모두 금지한다.
