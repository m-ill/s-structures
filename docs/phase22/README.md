# Phase22 조회·상태 / 성능·메모리 / 자동 PDF

2026-09-11 · 기준 실행 코드 931dce1, 문서 기준 971b5ca. 사용자 요청에 따라 실제 건물·현장 자료를 사용하지 않는다. 전체 회귀와 장시간 부하 시험을 실행하지 않고 합성 자료의 변경 기능 집중 테스트만 수행한다.

| 순서 | 코드 작업 | 최소 검증 |
|---|---|---|
| M0 조회·상태 | 워크플로 실행 job과 직접 실행 job의 조회 연결. 복원 결과는 새 세션 조회 ID 발급. 입력 identity로 상태 계산, 과거 세션 권한 유지 금지 | 합성 workflow 조회, 복원 조회, 수정 후 stale, 잘못된 ID·경로 거부 |
| M1 성능·메모리 | 목록·상태 조회에서 전체 결과 복제를 제거하고 metadata/부분 조회 적용. 관리 메모리 추정 임시 배열 제거. 실제 heap와 관리 ledger를 구분한 진단 제공 | 큰 합성 결과의 작은 조회, 반환값 격리, 예산·dispose 확인 |
| M2 자동 PDF | 브라우저에서 예비 검토 기록 PDF를 자동 생성·다운로드. 한글, 페이지 분할, 입력·결과 hash와 NG/미검토 표시. 페이지/메모리 예산 및 URL 해제 | 작은 합성 기록의 PDF 형식·페이지·한도·stale 차단 검사 |

예비 검토 PDF와 기존 최종 계산서 PDF는 서로 다른 출력 범위다. 기존 7개 그림·최종 자격 gate를 완화하지 않는다. NG 결과도 예비 기록으로 출력하되 최종 설계 승인으로 표시하지 않는다. 외부 라이브러리·네트워크·프린트 대화상자에 의존하지 않는 브라우저 출력 경로를 추가한다.

수치 알고리즘 변경·프로덕션 성능 인증·실제 건물 검증·배포는 이번 작업에 포함하지 않는다. 완료 상태와 실행한 집중 테스트는 STATUS.md에 기록한다.
